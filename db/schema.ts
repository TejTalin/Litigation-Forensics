import { jsonb, pgTable, text, timestamp, uuid, integer } from 'drizzle-orm/pg-core';

export const cases = pgTable('cases', { id: uuid('id').primaryKey(), name: text('name').notNull(), caseNumber: text('case_number'), court: text('court'), createdAt: timestamp('created_at', { withTimezone: true }).notNull() });
export const documents = pgTable('documents', { id: uuid('id').primaryKey(), caseId: uuid('case_id').references(() => cases.id), filename: text('filename').notNull(), extractedText: text('extracted_text'), extractionMethod: text('extraction_method'), createdAt: timestamp('created_at', { withTimezone: true }).notNull() });
export const scanResults = pgTable('scan_results', { id: uuid('id').primaryKey(), documentId: uuid('document_id').references(() => documents.id), module: text('module').notNull(), result: jsonb('result').notNull(), createdAt: timestamp('created_at', { withTimezone: true }).notNull() });
export const courtQueueWatches = pgTable('court_queue_watches', { id: uuid('id').primaryKey(), courtName: text('court_name').notNull(), courtNumber: text('court_number').notNull(), userItemNumber: integer('user_item_number').notNull(), status: text('status').notNull(), boardUrl: text('board_url'), parserRule: jsonb('parser_rule'), runningItemNumber: integer('running_item_number'), lastCheckedAt: timestamp('last_checked_at', { withTimezone: true }), createdAt: timestamp('created_at', { withTimezone: true }).notNull(), updatedAt: timestamp('updated_at', { withTimezone: true }).notNull() });
